<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="killbit.taskrabbit">
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">

        <meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>


        <activity android:name=".actvity.Launcher"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/SpeedyTheme">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".actvity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MyMaterialTheme"
            android:screenOrientation="sensorPortrait">

        </activity>

        <activity
            android:name=".actvity.Signup"
            android:label="@string/app_name"
            android:theme="@style/MyMaterialTheme"
            android:screenOrientation="sensorPortrait">

        </activity>

        <activity
            android:name=".actvity.Signup_email"
            android:label="@string/app_name"
            android:theme="@style/MyMaterialTheme"
            android:screenOrientation="sensorPortrait">

        </activity>
        <activity
            android:name=".actvity.SignIn_email"
            android:label="@string/app_name"
            android:theme="@style/MyMaterialTheme"
            android:screenOrientation="sensorPortrait">

        </activity>
        <activity
            android:name=".actvity.Login"
            android:label="@string/app_name"
            android:theme="@style/MyMaterialTheme"
            android:screenOrientation="sensorPortrait">

        </activity>


    </application>

</manifest>